import Routing from "../../LayoutArea/Routing/Routing";
import "./Main.css";

function Main(): JSX.Element {
    return (
        <div className="Main">
			I am main!
            <Routing/>
        </div>
    );
}

export default Main;
